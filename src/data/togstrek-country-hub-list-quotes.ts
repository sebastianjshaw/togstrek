/**
 * Optional pull-quote shown on continent hub country tiles (`/{continent}`).
 * Keyed by ISO 3166-1 alpha-2. Add entries as copy is ready; omitted countries
 * show title only.
 */
export const togstrekCountryHubListQuoteByIso2: Partial<
  Record<string, string>
> = {
  AR: "We are Argentina. Who the opponent is doesn't matter.",
};
