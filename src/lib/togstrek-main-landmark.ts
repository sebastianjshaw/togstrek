/** Skip-link target (`TogstrekSkipLink`) — one per route `<main>`. */
export const TOGSTREK_MAIN_ID = "togstrek-main";

export const togstrekMainLandmarkProps = {
  id: TOGSTREK_MAIN_ID,
  tabIndex: -1,
} as const;
