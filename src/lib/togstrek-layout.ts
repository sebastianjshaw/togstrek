/** Default vertical padding for full-width page sections (main column). */
export const TOGSTREK_PAGE_SECTION_Y =
  "py-[var(--tt-space-16)]" as const;

/** Inner content blocks below a hero (place pages, etc.). */
export const TOGSTREK_PAGE_CONTENT_Y =
  "py-[var(--tt-space-12)] md:py-[var(--tt-space-16)]" as const;
