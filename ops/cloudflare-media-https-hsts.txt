Cloudflare checklist for Security Insights items on media.togstrek.com (and consistency with the apex zone). Apply in the Cloudflare dashboard for the togstrek.com zone.

1) SSL/TLS → Overview
   - Encryption mode: Full (strict) if origin supports valid TLS (R2 public hostname, Workers, or your origin as applicable).

2) SSL/TLS → Edge Certificates
   - Enable "Always Use HTTPS" (HTTP → 301 to HTTPS on port 80). Zone-wide; covers media.togstrek.com if the hostname is proxied (orange cloud).

3) SSL/TLS → Edge Certificates → HTTP Strict Transport Security (HSTS)
   - Enable for the zone (or confirm already on). max-age ≥ six months is typical; includeSubDomains only if every hostname under togstrek.com is HTTPS-ready.

4) DNS → media.togstrek.com
   - Ensure the record is proxied (orange cloud) if traffic should get these edge settings. DNS-only (grey) records bypass Cloudflare TLS/redirect features.
   - Verified 2026-05-04: R2 record `media.togstrek.com` → `togstrek-media`, Proxied (orange). Point 4 satisfied.
   - Note: `togstrek.com` and `www` are DNS-only (grey). Cloudflare items (1–3) apply at the edge only for orange names (e.g. `media`, `_domainconnect`); HTML traffic for www/apex goes straight to Vercel.

5) After changes, re-run Security Insights or probe:
   curl -sI http://media.togstrek.com/ | head -5
   Expect: 301 to https://... (when Always Use HTTPS is on).

Note: The public site (www) on Vercel already sends Strict-Transport-Security; this checklist addresses the media hostname called out in the export.
